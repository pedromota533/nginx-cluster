apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-config
  namespace: nginx
data:
  default.conf: |
    server {
      listen 80;
      server_name nginx.wsl.local;

      location / {
        root /usr/share/nginx/html;
        index index.html;
      }
    }
